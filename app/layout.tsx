import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import './globals.css'

export const metadata: Metadata = {
  title: 'Hyderabad Lakes - Water Body Conservation Story',
  description: 'Interactive exploration of Hyderabad\'s water bodies, their conservation status, and environmental challenges. Created by Farhat.',
  author: 'Farhat',
  keywords: ['Hyderabad', 'lakes', 'water conservation', 'environment', 'urban planning'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body>{children}</body>
    </html>
  )
}
